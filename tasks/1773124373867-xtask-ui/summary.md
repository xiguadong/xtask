- 完成任务页筛选与排序交互优化：排序条收敛为紧凑工具条，支持按优先级 / 时间 / 名称切换正序与倒序。
- 完成标签筛选体验优化：标签统一彩色徽标样式，选中态更明显，头部信息收敛为“全部 / 已选 N / 重置”。
- 修复标签大小写不一致问题：前端、后端与 CLI 统一将标签归一为小写，新增标签时自动转小写。
- 修复创建时间排序方向定义，并将任务页筛选、标签、排序、视图状态同步到 URL，刷新页面与从任务详情返回时不丢失上下文。
- 更新 `docs/task.md` 与 `PROGRESS.md`，并完成前端构建验证。
