# 交付物验收矩阵

| 交付项 | 用户任务 | 交互载体(硬件/软件) | 对应功能 | 触发输入 | 可观察输出 | 指标 | 测试方法 | 测试场景/数据 | 通过阈值 | 验收证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| 项目与任务状态流转（MVP） | 创建任务并按统一流程完成 | 浏览器/Web | 任务创建、编辑、状态流转 | 新建任务并依次变更状态 | 每次变更后状态、时间戳、历史记录可见 | 关键流程通过率 | 端到端回放 + 功能测试 | 单项目 30 条任务（不同优先级） | 关键路径连续 10 次回放通过 | 录屏、状态日志、测试报告 |
| 看板与列表视图 | 在不同视图下追踪任务进度 | 浏览器/Web | 看板列、列表筛选、排序 | 切换视图并设置筛选/排序条件 | 两种视图展示结果一致且可追踪 | 视图一致性通过率 | 功能测试 + 可用性走查 | 30/300 条任务数据集 | 视图一致性检查 100% 通过 | 截图、查询日志、测试记录 |
| Milestones 对齐能力 | 创建里程碑并追踪阶段完成度 | 浏览器/Web | 里程碑创建、关联、进度聚合 | 新建里程碑并绑定任务 | 里程碑进度随任务状态变化实时更新 | 聚合正确率 | 聚合逻辑测试 + 功能测试 | 多里程碑、多状态任务组合数据 | 进度计算校验样本 100% 正确 | 进度截图、校验记录、测试报告 |
| 任务检索与筛选 | 快速定位目标任务 | 浏览器/Web | 关键词检索、标签/优先级筛选 | 输入关键词并设置过滤条件 | 结果集符合输入条件且可复现 | 检索命中正确率 | 功能测试 | 命名相似任务与多标签任务集 | 预置查询用例全部通过 | 查询结果截图、用例报告 |
| 任务关系表达（MVP） | 为任务建立父子/依赖/关联关系 | 浏览器/Web | 关系创建、关系校验、关系展示 | 在任务详情设置父任务、依赖任务或关联任务 | 关系在详情页可见，看板卡片显示关系徽标；循环依赖被阻止；被阻塞任务不可完成；强/弱耦合可区分展示 | 关系建模通过率 | 功能测试 + 规则测试 | 树状拆解、依赖链、强耦合/弱耦合关联样本 | 关系规则用例全部通过 | 关系截图、校验日志、测试报告 |
| 新建任务交互（MVP） | 用快速创建或高级创建建立任务 | 浏览器/Web | New Task、Add Subtask、创建表单 | 从看板点击 `New Task` 或在详情点击 `Add Subtask` | 必填字段校验正确；默认值生效；任务创建成功并可立即显示 | 创建成功率 | 端到端回放 + 表单校验测试 | 空值/合法值/边界值输入集 | 创建流程连续 20 次通过 | 录屏、表单校验日志、测试报告 |
| 轻量任务文件持久化（MVP） | 在项目目录持久保存任务、关系与规则 | 浏览器/Web + 本地文件系统 | `task_graph.yaml` 读写、原子更新；`task_rule_doc.md` 自动生成 | 创建/更新任务后触发保存 | `<project_dir>/.xtask/task_graph.yaml` 内容同步更新且格式正确，且 `.xtask/task_rule_doc.md` 存在 | 持久化成功率 | 文件读写测试 + 端到端回放 | 连续创建/更新 50 次任务操作 | 文件写入无损坏、无丢字段 | 文件快照、操作日志、测试报告 |
| 两级页面信息流闭环（D） | 从首页总览进入子页并完成任务推进闭环 | 浏览器/Web | 首页聚合、子页执行、详情抽屉联动 | 在首页筛选项目并进入子页推进任务状态 | 首页统计、子页看板、详情抽屉、里程碑进度保持一致并可回溯 | 闭环一致性通过率 | 端到端回放 + 一致性校验 | 3 条“首页->子页->返回首页”闭环路径 | 闭环路径 100% 通过且无状态漂移 | 回放录屏、一致性日志、验收记录 |
| 布局与风格一致性验收 | 对照基线验证实现界面 | 浏览器/Web | 两级页面布局检查、自检脚本、截图对照 | 执行自动自检并进行人工终检 | 一级首页展示项目概要看板；二级子页展示项目任务详情；关键分区结构一致 | 布局一致性通过率 | 自动自检 + 人工检查 + 截图对照 | 视口 1366x768 与 390x844；首页总览、项目子页、详情展开、创建流程 | 自动自检 >=90% 且关键路径 100%；结构阻塞偏差 0；人工风格终检通过 | 自检报告、对照截图、人工确认记录 |
| UI-UX 自检机制（MVP） | 自动验证大部分功能与界面一致性 | 浏览器/Web + 自动化测试 | 结构检查、行为检查、视觉检查、可访问性检查 | 运行自检套件 | 输出可追溯自检报告（通过率、失败项、截图、日志） | 自检覆盖率 | 自动化测试 + 截图比对 + a11y 检查 | 首页总览、项目子页、新建任务、关系跳转、阻塞校验 | 覆盖率 >=90%，关键路径项 100% 通过 | 自检报告、失败快照、修复记录 |
| 设计系统主从治理（MVP） | 按 Master + Page Override 一致实现页面 | 浏览器/Web + 文档 | `MASTER.md` 与 `pages/*.md` 层级继承 | 开发页面前读取对应 page override 文件 | 页面实现与设计系统规则一致，未覆盖项正确回落 Master | 规则一致性通过率 | 文档对照检查 + 页面审阅 | 首页与项目子页两类页面 | 关键规则一致性 100% 通过 | 对照清单、审阅记录、差异说明 |
